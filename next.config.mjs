/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'encrypted-tbn0.gstatic.com'], // Add 'encrypted-tbn0.gstatic.com' to the list of domains
    },
};

export default nextConfig;
